import styled from "styled-components"

export const TimesheetTable = styled.table.attrs({
  className: "table table-borderless"
})`
	font-family: 'Inter';
	font-style: normal;
	font-weight: 500;
	font-size: 12px;

`